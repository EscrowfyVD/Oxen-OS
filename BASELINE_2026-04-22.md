# Baseline santé repo — 2026-04-22 (pré-Sprint 2.1)

État du repo avant activation de la CI GitHub Actions. Les chiffres ci-dessous sont la référence pour décider de ce qui bloque / ne bloque pas la CI au premier push.

## Versions

| Outil | Version |
|---|---|
| Node (local) | v24.14.0 |
| Node (Railway / CI) | v22.x (pinned) |
| npm | 11.9.0 |
| Next.js | 16.1.6 |
| Prisma | 5.22.0 |
| TypeScript | ^5 |
| Vitest | 4.1.5 |
| ESLint | 9.x (flat config via `eslint.config.mjs`) |

## Checks (résultats exacts)

| Check | Exit code | Problèmes | Status |
|---|---:|---|:---:|
| `tsc --noEmit` (monolith) | 0 | 0 | ✅ |
| `tsc --noEmit` (sync-worker) | 0 | 0 | ✅ |
| `tsc --noEmit` (ai-worker) | 2 | 1 error | ❌ |
| `npm run build` (next build) | 0 | 1 warning pré-existant | ✅ |
| `npx vitest run` | 0 | 19/19 tests passing (2 files) | ✅ |
| `npx eslint .` | 1 | 79 errors + 157 warnings (236 total) | ❌ |

## Erreurs / warnings notables

### `tsc --noEmit` (monolith) ✅
Passe sans erreur.

### `tsc --noEmit` (sync-worker) ✅
Passe sans erreur. Le fichier `workers/sync-worker/src/lib/prisma.ts` (synchronisé depuis `src/lib/prisma.ts` au Sprint 1.3) compile correctement avec `$extends`.

### `tsc --noEmit` (ai-worker) ❌
Une erreur pré-existante :
```
src/index.ts(56,34): error TS2322:
Type 'Record<string, unknown>' is not assignable to type
'NullableJsonNullValueInput | InputJsonValue | undefined'.
```
Context : passage d'un objet générique à un champ Prisma `Json`. Pas introduit par les sprints récents. À fixer dans un sprint cleanup dédié.

### `npm run build` (next build) ✅
Build Next.js 16.1.6 passe. Un seul warning connu :
- `⚠ Next.js inferred your workspace root...` — lié à plusieurs lockfiles dans les dossiers parents (`Oxen os/` contient aussi `Escrowfy-OS/`). **Pré-existant**, documenté au Sprint 1.2 (migration proxy).
  Fix : ajouter `turbopack.root` dans `next.config.ts`. Non urgent.

### `npx vitest run` ✅
Tous les tests passent :
- `src/lib/token-encryption.test.ts` — 17 tests (round-trip, idempotence, tampering, key validation, nullable)
- `src/lib/__tests__/worker-sync.test.ts` — 2 tests (SHA-256 coherence monolith ↔ sync-worker)

### `npx eslint .` ❌
**236 problèmes au total** (79 errors, 157 warnings).

Note importante : `next lint` n'existe plus dans Next.js 16 — supprimé de la CLI (`npx next lint --help` ne le liste pas). On appelle `eslint` directement, avec la flat config déjà en place (`eslint.config.mjs`).

Catégories d'erreurs (échantillon sur les 79) :
- `@typescript-eslint/no-explicit-any` — usage de `any` (plusieurs dizaines)
- `@typescript-eslint/no-unused-vars` — variables définies non utilisées (~150 warnings)
- `react-hooks/set-state-in-effect` — appels `setState` synchrone dans `useEffect` (nouveau rule React 19)

Aucune de ces erreurs n'affecte la production — le code tourne. Elles reflètent un style de code à aligner dans un sprint lint cleanup dédié. **Hors scope Sprint 2.1.**

## Décisions pour la CI

CI configurée **en mode progressif** (principe : la CI doit être verte au premier push, sinon elle ne sera pas prise au sérieux) :

| Check | Blocant CI ? | Justification |
|---|:---:|---|
| `tsc --noEmit` (monolith) | ✅ Oui | Baseline verte, protège contre régression |
| `tsc --noEmit` (sync-worker) | ✅ Oui | Baseline verte |
| `tsc --noEmit` (ai-worker) | ❌ Skippé | Baseline rouge (1 erreur pré-existante). TODO commenté dans `ci.yml` pour réactivation post-cleanup |
| `next build` | ✅ Oui | Baseline verte |
| `vitest run` | ✅ Oui | Baseline verte (19/19) |
| `eslint .` | ⚠️ Non-blocant (`continue-on-error: true`) | Baseline rouge (79 errors). Visibilité uniquement jusqu'au sprint lint cleanup |

**Progression future** : une fois les 2 TODO du `.github/workflows/ci.yml` résolus (ai-worker TS error + lint errors = 0), retirer `continue-on-error` et décommenter l'étape ai-worker pour passer en CI strict.
